<% content_for :titulo_pagina do %>iniciar sesión<% end %>
<% content_for :titulo_bloque do %>iniciar sesión<% end %>
<%# content_for :head do stylesheet_link_tag "form", :media => "all" end %>

<%= form_for(resource, as: resource_name, url:  @path ) do |f| %>
  <div id="campo_cuenta_login"><%= f.label :login %><br />
  <%= f.text_field :login, autofocus: true %></div>

  <div id="campo_cuenta_password"><%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off" %></div>

  <% if devise_mapping.rememberable? -%>
    <div id="campo_cuenta_remember_me"><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
  <% end -%>

  <div style="text-align:center" id="campo_cuenta_commit"><%= f.submit "iniciar sesión" %></div>
<% end %>

<%= render "devise/shared/links" %>
